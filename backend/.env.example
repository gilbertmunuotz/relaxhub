# Copy to .env and customize for your machine.
# cp .env.example .env

# Database — use your local PostgreSQL user
DB_URL=jdbc:postgresql://localhost:5432/relaxhub
DB_USERNAME=your_macos_username
DB_PASSWORD=

# JWT (Step 2+)
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRATION_MS=86400000
