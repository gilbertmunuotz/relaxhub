# Copy to .env and add your keys (this file is gitignored).
# cp .env.example .env

# Google Maps — enable "Maps SDK for Android" in Google Cloud Console
# Restrict the key to your app package + SHA-1 fingerprint.
GOOGLE_MAPS_API_KEY=your_maps_api_key_here
